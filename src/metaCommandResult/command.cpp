#include<iostream>
#include "./command.h"
#include "../../lib/termcolour.hpp"
#include<string>
using namespace std;

/**
 * @method of Command
 * 
 * @description - This function checks whether te input by the user is valid or not if the 
 *                input is invalid it returns false else true
 * 
 * @return - boolean indicating whether the command is valid or not
 * 
 */
bool Command::checkCommand(){

}